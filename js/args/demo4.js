const err=(message)=>{
    throw new Error(message);
}
function sum(num= err('first param is not defined'),otehrNum= err('second param is not defined')){
    return sum;
}
console.log(underfind,10);
// 报错 第一个参数没有定义
console.log(10);
// 报错，第二个参数没有定义